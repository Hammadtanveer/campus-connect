{
  "name": "campusconnect-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for CampusConnect: set admin custom claims when valid admin code is provided.",
  "main": "index.js",
  "engines": {
    "node": "18 || 20 || 22"
  },
  "scripts": {
    "lint": "eslint . || true",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.4.0"
  }
}

